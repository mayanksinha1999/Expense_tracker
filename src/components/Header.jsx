import { color } from '@mui/system'
import React from 'react'

const Header = () => {
  return (
    <div style={{backgroundColor:'black', color:'white', padding: '14px',borderRadius: '5px'}}>
      Expense Tracker
    </div>
  )
}

export default Header
